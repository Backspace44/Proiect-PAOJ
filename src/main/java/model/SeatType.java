package main.java.model;

/**
 * Enum representing different types of seats in a venue.
 * Used to categorize seats by quality/features for pricing and reservation purposes.
 */
public enum SeatType {
    REGULAR,  // Standard seating with normal amenities
    VIP       // Premium seating with extra benefits/comfort
}